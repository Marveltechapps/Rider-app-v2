/**
 * Profile Photo Screen Route
 * Entry point for the selfie capture screen
 */

import ProfilePhotoScreen from '../src/screens/ProfilePhotoScreen';

export default function ProfilePhoto() {
  return <ProfilePhotoScreen />;
}

